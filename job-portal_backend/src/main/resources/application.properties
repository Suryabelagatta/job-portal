spring.application.name=job-portal


spring.datasource.url=jdbc:mysql://localhost:3306/job_portal?useSSL=false
spring.datasource.username=root
spring.datasource.password=Surya123$
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --------------------------------------
# JPA & Hibernate Configuration
# --------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# --------------------------------------
# Multipart (File Upload) Configuration
# --------------------------------------
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB

# Custom Resume Upload Directory
file.upload-dir=src/main/resources/static/uploads/

# --------------------------------------
# Logging
# --------------------------------------
logging.level.org.springframework=INFO
logging.level.com.job-portal=DEBUG

# --------------------------------------
# CORS (Optional if WebConfig handles it)
# --------------------------------------
# (Handled in WebConfig.java - no need to specify here)

# --------------------------------------
# Security / JWT Properties (Optional - if using JWT)
# --------------------------------------
# jwt.secret=your_jwt_secret_key
# jwt.expiration=3600000
spring.web.resources.static-locations=file:/Users/suryabalegatta/Job-Portal/job-portal_backend/src/main/resources/static/
